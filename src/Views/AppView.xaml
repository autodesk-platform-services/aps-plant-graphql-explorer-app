<UserControl x:Class="GraphQLClient.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:v="clr-namespace:GraphQLClient.Views"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary x:Uid="ResourceDictionary_2" Source="Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid KeyboardNavigation.TabNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0"
                   Text="Plant 3D AEC DM Explorer"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 24" />

        <DockPanel Grid.Row="1"
                   HorizontalAlignment="Stretch"
                   Margin="12, 5, 12, 0">
            <TextBlock Text="{Binding ViewTitle}" 
                       DockPanel.Dock="Left"
                       VerticalAlignment="Bottom"/>

            <Button 
                x:Name="freshViewButton"
                Content="Refresh View"
                Width="120"
                DockPanel.Dock="Right"
                Click="freshViewButton_Click">
                <Button.Style>
                    <Style BasedOn="{StaticResource OutlineButtonStyle}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewType}" Value="{x:Static v:ViewTypes.Search}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <ComboBox x:Name="GraphQLRegionCombobox"
                      Width="120"
                      DockPanel.Dock="Right"
                      Margin="0,0,12,0"
                      SelectedIndex="0"
                      SelectionChanged="GraphQLRegionCombobox_SelectionChanged">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewType}" Value="{x:Static v:ViewTypes.Hubs}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ComboBox.Style>
            </ComboBox>

            <Grid />
        </DockPanel>

        <Grid x:Name="ViewGrid"
                  Grid.Row="2"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="12"/>

        <DockPanel Grid.Row="3" HorizontalAlignment="Stretch" Margin="12,0,12,5">
            <Button x:Name="backButton"
                Content="{Binding BackButtonTitle}"
                Width="138"
                DockPanel.Dock="Left"
                Click="backButton_Click">
                <Button.Style>
                    <Style BasedOn="{StaticResource OutlineButtonStyle}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewType}" Value="{x:Static v:ViewTypes.Hubs}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ParentView}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button x:Name="nextButton"
                    Content="{Binding NextButtonTitle}"
                    Width="120"
                    DockPanel.Dock="Right"
                    Style="{StaticResource OutlineButtonStyle}"
                    Click="nextButton_Click">
            </Button>

            <Grid/>
        </DockPanel>
    </Grid>
</UserControl>


